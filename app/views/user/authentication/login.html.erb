<%= 'Unknown Student ID' if @student_id %>
<%= form_tag('/login') do
  label_tag(:student_id, 'Student ID') <<
      text_field_tag(:student_id, nil, required: true) <<
      submit_tag('Submit')
end %>
